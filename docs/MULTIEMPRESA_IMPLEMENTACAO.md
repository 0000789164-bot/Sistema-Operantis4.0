# Implementação multiempresa

A branch `login-multiempresa` contém a camada de autenticação por e-mail/senha, cadastro inicial da empresa, vínculo usuário → empresa e persistência operacional em `empresas/{empresaId}/dados/operacional`.

A integração do `index.html` é aplicada pelo workflow `enable-multiempresa.yml` nesta branch.
